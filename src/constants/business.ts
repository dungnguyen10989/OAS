import { colors } from './colors';
export const STATUS = ['Waiting', 'Return', 'Cancelled', 'Pending'];

export const COLORS_STATUS = [
  colors.status.waiting,
  colors.status.return,
  colors.status.cancel,
  colors.status.pending
];
